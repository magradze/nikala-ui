name: Nikala UI Workspace Quality Gate

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  workspace-build:
    name: Build Packages & Web Portal
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Bun Environment
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install Workspace Dependencies
        run: bun install --frozen-lockfile

      - name: Build Core, CLI & Web Application
        run: bun run build

      - name: Verify No Build Drift in Registry
        run: git diff --exit-code packages/core/registry

  test-suite:
    name: Run Unit & Integration Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Bun Environment
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install Workspace Dependencies
        run: bun install --frozen-lockfile

      - name: Build Core Registry Manifests
        run: bun run build:registry

      - name: Execute Vitest Test Suites
        run: cd apps/web && bunx vitest run

  type-check:
    name: Validate Monorepo TypeScript Types
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Bun Environment
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install Workspace Dependencies
        run: bun install --frozen-lockfile

      - name: Build Core Registry Manifests
        run: bun run build:registry

      - name: Typecheck Core Package
        run: cd packages/core && bunx tsc --noEmit

      - name: Typecheck CLI Package
        run: cd packages/cli && bunx tsc --noEmit

      - name: Typecheck Web App
        run: cd apps/web && bunx tsc --noEmit