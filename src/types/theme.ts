/* Re-export theme types directly from Nikala UI provider without modifying original files */
export type {
  Theme,
  AccentColor,
  Radius,
  ThemeProviderProps,
} from "@/providers/theme-provider";

export type { ThemeToggleProps } from "@/components/ui/theme-toggle";