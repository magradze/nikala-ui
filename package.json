{
  "name": "nikala-ui-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:registry": "cd packages/core && bun run build:registry",
    "build:cli": "cd packages/cli && bun run build",
    "build": "bun run build:registry && bun run build:cli"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}